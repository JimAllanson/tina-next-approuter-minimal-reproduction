{
  "name": "tina-next-minimal-example",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "TINA_PUBLIC_IS_LOCAL=true tinacms dev -c \"next dev\"",
    "build": "tinacms build && next build",
    "build:local": "tinacms build --local --skip-cloud-checks --verbose -c \"next build\"",
    "start": "next start",
    "lint": "next lint"
  },
  "dependencies": {
    "next": "14.1.3",
    "react": "^18",
    "react-dom": "^18",
    "tinacms": "^1.6.0"
  },
  "devDependencies": {
    "@tinacms/cli": "^1.5.42"
  }
}